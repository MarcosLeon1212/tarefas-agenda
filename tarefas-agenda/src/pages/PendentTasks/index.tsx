import React from "react";
import { View, Text, TextInput, ScrollView, TouchableOpacity} from "react-native";


export const PendentTasksScreen = () => {

    return(
        <ScrollView>
                


                
        </ScrollView>
    )
}